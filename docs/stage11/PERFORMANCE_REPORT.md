# Performance Report — Stage 11

Interaction loops target &lt;250ms. E2E voice turns remain LLM-bound (3–6s p50 soft target). Streaming progressive reveal, audio backpressure, and quality adaptation reduce perceived latency and dropped audio under load. See `PERFORMANCE_GUIDE.md` and `LATENCY_BUDGET.md`.
