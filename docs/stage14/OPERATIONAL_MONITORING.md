# Operational Monitoring

**Surfaces:** CIDP system panel · Stage 12 `buildProductionOpsSnapshot` · enterprise observability · `docs/cidp/monitoring/`

## Continuously monitor

System uptime · API latency · Realtime / SSE latency · Voice E2E latency · Streaming quality · Queue utilization · Resource consumption · Deployment health · Database performance · Infrastructure stability · Error rates · Service availability

## Executive dashboards

Configured in `../cidp/monitoring/DASHBOARD_CONFIG.json` with alert catalog `ALERTS.json`. Admin KPIs on `/admin/cidp`.

## Weekly output

`operations` kind from `buildWeeklyReports()`.
